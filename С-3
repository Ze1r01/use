#include <stdio.h>

int main() {
    int a, b, c;
    printf("Введите три числа (a, b, c): ");
    scanf("%d %d %d", &a, &b, &c);
    
    printf("%d\n", ((a < 0) + (b < 0) + (c < 0)) % 2 == 0 ? 1 : -1); // 1=положительный , -1 отрицательный 
    
    return 0;
}
